<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Pure black window: on the X3 Pro waveguide, black is transparent, so
         the neon court floats in the real world and the projector rests. -->
    <style name="Theme.X3Pooyan" parent="android:Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
